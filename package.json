{
  "name": "hostel-app",
  "version": "0.10.0",
  "description": "Hostel Guest Portal with Energy Monitoring",
  "scripts": {
    "dev": "npx serve dashboard -p 8080",
    "build": "echo 'Static site - no build needed'",
    "preview": "npx serve dashboard -p 3000",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:build": "npx cap sync ios && npx cap open ios"
  },
  "keywords": [
    "hostel",
    "guest-portal",
    "energy",
    "monitoring",
    "shelly",
    "dashboard"
  ],
  "author": "SSI",
  "license": "MIT",
  "dependencies": {
    "@capacitor/cli": "^8.0.1",
    "@capacitor/core": "^8.0.1",
    "@capacitor/ios": "^8.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
